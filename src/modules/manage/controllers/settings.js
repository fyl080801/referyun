define('modules.manage.controllers.settings', [
    'modules.manage.module'
], function (module) {
    'use strict';

    module.controller('modules.manage.controllers.settings', [
        '$scope',
        function ($scope) {

        }
    ]);
});